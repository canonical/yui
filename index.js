// CommonJS entry point. Mirrors the historical `yui` npm package: requiring the
// package yields the Node seed which exports `{ YUI }`.
module.exports = require('./build/yui-nodejs/yui-nodejs.js');
